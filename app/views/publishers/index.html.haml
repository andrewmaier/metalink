%h2 Listing Publishers

%table
  %tr

  - for publisher in @publishers
    %tr

      %td= link_to 'Show', publisher
      %td= link_to 'Edit', [:edit, publisher]
      %td= link_to 'Destroy', publisher, :method => :delete, :confirm => 'Are you sure?'

= link_to 'New Publisher', new_publisher_path
